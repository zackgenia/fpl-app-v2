{
  "name": "fpl-transfer-recommender",
  "version": "2.0.0",
  "description": "Advanced FPL Transfer Recommendation Tool",
  "main": "server/index.js",
  "scripts": {
    "start": "node server/index.js",
    "build": "cd client && npm install && npm run build",
    "test": "node --test"
  },
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
